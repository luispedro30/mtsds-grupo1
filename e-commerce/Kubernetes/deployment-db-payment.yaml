apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payment-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

---
apiVersion: v1
kind: Service
metadata:
  name: bd-payment
spec:
  selector:
    app: bd-payment
  ports:
    - protocol: TCP
      port: 5438
      targetPort: 5432
  clusterIP: None

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bd-payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bd-payment
  template:
    metadata:
      labels:
        app: bd-payment
    spec:
      containers:
        - name: bd-payment
          image: postgres:16.0
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: payment
            - name: POSTGRES_USER
              value: admin
            - name: POSTGRES_PASSWORD
              value: postgres123
      volumes:
        - name: payment-data
          persistentVolumeClaim:
            claimName: payment-pvc
